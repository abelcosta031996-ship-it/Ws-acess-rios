{
  "name": "ws-acessorios",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node scripts/copy-static.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node scripts/verify-catalog.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
